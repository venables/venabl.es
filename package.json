{
  "name": "venables.github.io",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "build": "parcel build src/index.html --public-url .",
    "clean": "rimraf .cache dist tmp",
    "deploy": "git push origin `git subtree split --prefix dist`:master --force",
    "dev": "parcel src/index.html --public-url .",
    "release": "./bin/release",
    "start": "yarn dev"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/venables/venables.github.io.git"
  },
  "author": "Matt Venables",
  "license": "MIT",
  "devDependencies": {
    "parcel-bundler": "^1.12.3",
    "parcel-plugin-inliner": "^1.0.10",
    "rimraf": "^2.6.3"
  }
}
