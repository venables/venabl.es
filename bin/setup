#!/usr/bin/env sh

echo "✨ Installing dependencies ..."
npm install

echo "\n✨ Creating .env file..."
if [ -f .env ]; then
    echo "File already exists, skipping."
else
  cp .env.example .env
fi

echo "\n🎉 All set. Happy coding!"
